<table border = "1">
  <% @notes.each do |note| %>
    <tr>
      <td><%= note[:title] %></td>
      <td><%= note[:opinion] %></td>
      <td><%= note[:rating] %></td>
    </tr>
  <% end %> 
</table>

<br>
<%= link_to 'Add New', new_path %>
